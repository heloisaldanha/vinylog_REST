spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/vinylog
    username: postgres
    password: 123456
  jpa:
    hibernate:
      database-platform: org.hibernate.dialect.PostgreSQLDialect
      ddl-auto: update
  flyway:
    baselineOnMigrate: true